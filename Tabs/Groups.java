package Tabs;
import javax.swing.*;
import java.awt.*;

public class Groups extends JPanel{
    public Groups() {
        JLabel label = new JLabel("Groups of items", SwingConstants.CENTER);
        label.setFont(new Font("Century Gothic", Font.PLAIN, 16));
        this.add(label);
    }
}
